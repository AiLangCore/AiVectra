Program {
  Import(path="../../src/AiVectra/src/lib.aos")
  Export(name=start)

  Let(name=start) {
    Fn(params=args) {
      Block {
        Call(target=helloWindow) { Lit(value=0) }
        Return { Lit(value=0) }
      }
    }
  }
}
