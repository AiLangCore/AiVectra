#!/usr/bin/env bash
set -euo pipefail
ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
REAL_AILANG="$ROOT_DIR/.tools/ailang"
if [[ "${1:-}" == "debug" ]]; then
  shift
  exec "$ROOT_DIR/scripts/aivectra" --ailang "$REAL_AILANG" debug "$@"
fi
exec "$REAL_AILANG" "$@"
